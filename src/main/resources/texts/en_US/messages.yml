prefix: "<prefix> "
command:
  no_permission: "<prefix><error>You don't have permission."
  player_only: "<prefix><error>This command can only be used by players."
  unknown: "<prefix><error>Unknown subcommand."
  reload: "<prefix><success>NemoriaQuest reloaded."
  start:
    usage: "<prefix><primary>Usage: /nq start <questId>"
    started: "<prefix><success>Started quest <primary>{quest}</primary>."
    not_found: "<prefix><error>Quest <primary>{quest}</primary> not found."
    already_active: "<prefix><secondary>Quest <primary>{quest}</primary> is already active."
    limit_reached: "<prefix><error>Quest <primary>{quest}</primary> cannot be started (completion limit)."
    requirements: "<prefix><error>Requirements not met for quest <primary>{quest}</primary>."
    permission: "<prefix><error>You lack permission to start quest <primary>{quest}</primary>."
  stop:
    usage: "<prefix><primary>Usage: /nq stop <questId>"
    stopped: "<prefix><success>Stopped quest <primary>{quest}</primary>."
  active:
    none: "<prefix><secondary>No active quests."
    list: "<prefix><success>Active quests: <primary>{list}"
  list:
    none: "<prefix><secondary>No quests available."
    entry: "<primary>{id}</primary> <gray>-</gray> {name}"
  info:
    usage: "<prefix><primary>Usage: /nq info <questId>"
    not_found: "<prefix><error>Quest <primary>{quest}</primary> not found."
    no_description: "<secondary>No description."
    entry: "<success>[{id}] <primary>{name}</primary><dark_gray>: </dark_gray><secondary>{desc}"
    objective: "<dark_gray>-</dark_gray> <primary>#{index}</primary> <secondary>{desc}"
    max_completions: "<secondary>Max completions:</secondary> <primary>{count}"
    requirements: "<secondary>Requires:</secondary> <primary>{reqs}"
    requirements_none: "<secondary>Requires:</secondary> <primary>none"
  complete:
    usage: "<prefix><primary>Usage: /nq complete <questId>"
    done: "<prefix><success>Marked quest <primary>{quest}</primary> as completed."
  progress:
    usage: "<prefix><primary>Usage: /nq progress <questId> <objectiveId>"
    done: "<prefix><success>Completed objective <primary>{objective}</primary> in quest <primary>{quest}</primary>."
  debug:
    usage: "<prefix><primary>Usage: /nq debug <on|off>"
    enabled: "<prefix><success>Debug enabled."
    disabled: "<prefix><secondary>Debug disabled."
scoreboard:
  no_objective: "<secondary>No objective"
  empty: "<secondary>Nothing to display"
help:
  title: "<gold>NemoriaQuest</gold> <gray>v{version}</gray> <dark_gray>|</dark_gray> <gold>Config</gold> <gray>v{config}</gray>"
  lines:
    - "<primary>/nq start <id></primary> <secondary>- Start a quest"
    - "<primary>/nq stop <id></primary> <secondary>- Stop a quest"
    - "<primary>/nq active</primary> <secondary>- Show active quests"
    - "<primary>/nq list</primary> <secondary>- List available quests"
    - "<primary>/nq info <id></primary> <secondary>- Quest details"
    - "<primary>/nq complete <id></primary> <secondary>- Mark quest completed"
    - "<primary>/nq reload</primary> <secondary>- Reload config"
